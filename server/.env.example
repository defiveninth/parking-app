# Server
PORT=4000

# SQLite (path to .sqlite file)
# If relative, it is relative to the `server/` directory when you run the server.
SQLITE_PATH=./parking_app.sqlite

# JWT secret for auth tokens
JWT_SECRET=your-super-secret-key-change-in-production
